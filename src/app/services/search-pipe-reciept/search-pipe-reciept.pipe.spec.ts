import { SearchPipeRecieptPipe } from './search-pipe-reciept.pipe';

describe('SearchPipeRecieptPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPipeRecieptPipe();
    expect(pipe).toBeTruthy();
  });
});
