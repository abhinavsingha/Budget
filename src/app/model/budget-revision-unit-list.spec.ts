import { BudgetRevisionUnitList } from './budget-revision-unit-list';

describe('BudgetRevisionUnitList', () => {
  it('should create an instance', () => {
    expect(new BudgetRevisionUnitList()).toBeTruthy();
  });
});
