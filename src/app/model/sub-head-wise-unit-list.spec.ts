import { SubHeadWiseUnitList } from './sub-head-wise-unit-list';

describe('SubHeadWiseUnitList', () => {
  it('should create an instance', () => {
    expect(new SubHeadWiseUnitList()).toBeTruthy();
  });
});
